/** Organization workflow statuses (Phase 6 — matches DB check constraint). */
export const ORGANIZATION_STATUS = {
  PENDING: "pending",
  APPROVED: "approved",
  REJECTED: "rejected",
}

export const ORGANIZATION_STATUSES = Object.values(ORGANIZATION_STATUS)
